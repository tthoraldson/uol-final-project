services:
  text-to-music:
    build:
      context: ./backend/text-to-music
    networks:
      - model-network
    ports:
      - "8080:8080"


networks:
  frontend-network:
    driver: bridge

  model-network:
    driver: bridge